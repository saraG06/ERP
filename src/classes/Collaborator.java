package classes;

import classes.Worker;

public class Collaborator extends Worker {

    public Collaborator(int code, String firstName, String lastName, int year, int month, int day) {
        super(code, firstName, lastName, year, month, day);
    }
}
